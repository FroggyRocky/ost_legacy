
///discription is the first message in the chat///
const initialState = {
    discription:''
}

export default function tickets(state=initialState, action) {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}

